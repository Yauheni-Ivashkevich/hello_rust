// Attributes - meta data for the compiler
#![allow(unused)]

// main () is the entry point of Rust program
fn main() {
// Macros is Rust generte code at compile time and are 
// invoked with an exclamation mark (!)
    println!("Hello, world!");
}
